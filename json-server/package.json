{
  "name": "json-server-app",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
