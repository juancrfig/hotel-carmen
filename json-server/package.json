{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "build": "echo 'No build step needed'"
  }
}
